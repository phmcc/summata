% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fullfit.R
\name{finalize_column_names}
\alias{finalize_column_names}
\title{Finalize Column Names for Display}
\usage{
finalize_column_names(
  result,
  uni_raw,
  multi_raw,
  exponentiate,
  columns,
  metrics
)
}
\arguments{
\item{result}{Data.table with columns to rename. Expected to contain some
combination of: uni_effect, uni_p, multi_effect, multi_p.}

\item{uni_raw}{Raw univariable data.table used to determine effect type
by checking for presence of OR, HR, RR, or Coefficient columns.}

\item{multi_raw}{Raw multivariable data.table used to determine adjusted
effect type.}

\item{exponentiate}{Logical or \code{NULL}. If \code{TRUE}, forces 
exponentiated labels (OR, HR, RR). If \code{FALSE}, uses "Coefficient".
If \code{NULL}, auto-detects from raw data columns.}

\item{columns}{Character string indicating which columns are present
(\code{"both"}, \code{"uni"}, or \code{"multi"}). Used for context but
renaming is based on actual column presence.}

\item{metrics}{Character vector of metrics being displayed (\code{"effect"},
\code{"p"}, or both). Used for context.}
}
\value{
The input data.table with columns renamed
}
\description{
Renames internal column names (uni_effect, uni_p, multi_effect, multi_p) to
publication-ready display names with appropriate effect measure labels
(OR, HR, RR, aOR, aHR, aRR, Coefficient, etc.).
}
\keyword{internal}
