# Check LaTeX installation

Verifies that a LaTeX distribution (pdflatex or xelatex) is available on
the system for PDF compilation.

## Usage

``` r
check_latex()
```

## Value

Logical `TRUE` if LaTeX is available, `FALSE` otherwise.
